# chaos-parrot

Provider adapters for LLM backends. Translates chaos-abi into each provider's wire format and back. Anthropic, OpenAI, local models — the kernel speaks chaos-abi, parrots handle the dialects.
