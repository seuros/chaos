# chaos-mcp-protocol

Chaos-owned MCP wire contract. MCP servers depend on this crate for method
names, inbox URI, hostInfo shape, and the reserved review provenance `_meta`
key. The configured MCP entry name is not part of the protocol.
