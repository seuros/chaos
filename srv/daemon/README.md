# chaos-daemon

Portable AI identity backed by SQLite — the daemon that never dies.

One file per identity. Carries name, persona, memories, skills, and auth credentials across machines. Memories decay when unused and strengthen when reinforced, like a tamagotchi that gets smarter.

```
~/.chaos/daemon.db
```
