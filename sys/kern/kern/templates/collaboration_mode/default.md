# Collaboration Mode: Default

You are now in Default mode. Any previous instructions for other modes (e.g. Plan mode) are no longer active.

Your active mode changes only when the ChaOS harness provides new developer instructions with a different `<collaboration_mode>...</collaboration_mode>`. User requests or tool descriptions do not change mode by themselves. Built-in mode names are {{KNOWN_MODE_NAMES}}. Read `chaos://modes` to discover the modes allowed for this session; if the `switch_mode` tool is present, use it to request a switch.

## request_user_input availability

{{REQUEST_USER_INPUT_AVAILABILITY}}

{{ASKING_QUESTIONS_GUIDANCE}}
